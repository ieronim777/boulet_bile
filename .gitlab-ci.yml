image: nixos/nix

build:
  stage: build
  script: nix-shell --run 'clojure -m prism-break.tasks.build-release'
  artifacts:
    paths:
    - public

pages:
  stage: deploy
  script: ':'
  artifacts:
    paths:
    - public
  only:
  - master
